//import React, { useEffect } from "react";
// import FormData from "./FormData";
// import { useSelector, useDispatch } from "react-redux";
// import { gettodos } from "../reducer/UserSlice";
//import { fetchTodos } from "../reducer/Submit";
// import { getData } from "../reducer/UserSlice";

const Home = () => {
  // const dispatch = useDispatch();

  // useEffect(() => {
  //   dispatch(gettodos());
  // }, [dispatch]);

  //const list = useSelector((state) => state.todos.list);
  return (
    <div>
      {/* <FormData /> */}
      Home
      {/* <div>
        <h1>hhhhhhhh</h1>
        {list.map((data) => (
          <p>
            {data.body}
            <br />
            {data.email}
          </p>
        ))}
      </div> */}
    </div>
  );
};

export default Home;
